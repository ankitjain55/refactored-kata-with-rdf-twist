repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.1
    hooks:
      # Lint (with auto-fix)
      - id: ruff-check
        args: [--fix]

      # Format (Black-compatible formatter)
      - id: ruff-format